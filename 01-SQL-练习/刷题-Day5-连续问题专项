# 刷题记录 Day 5 | 2026-06-30

## 今日目标
- [x] 连续问题专项5道
- [x] 窗口函数复习

## 做题记录

| 题号 | 题目 | 知识点 | 结果 |
|------|------|--------|------|
| 6 | 连续登录 | LAG + 相邻间隔判断 | ✅ |
| 7 | 连续订单 | TIMESTAMPDIFF + 月份差 | ✅ |
| 8 | 连续增长 | LAG + 环比判断 | ✅ |
| 9 | 连续排名 | RANK + 连续分组 | ✅ |
| 10 | 连续空缺 | CROSS JOIN + LEFT JOIN + 连续判断 | ✅ |

## 关键收获

### 1. 连续问题核心方法
- **DATE_SUB + ROW_NUMBER**：判断连续自然日/月
- **LAG + TIMESTAMPDIFF**：判断相邻间隔，更灵活
- **CROSS JOIN**：生成全量数据（用户×月份）

### 2. 窗口函数对比
| 函数 | 场景 |
|------|------|
| ROW_NUMBER() | 连续编号，去重 |
| RANK() | 排名，跳号 |
| DENSE_RANK() | 排名，不跳号 |
| LAG/LEAD | 前后行对比 |
| TIMESTAMPDIFF | 日期差计算 |

### 3. 易错点
- `WITH` 最后一个CTE后面**不要逗号**
- `LAG` 第一行是NULL，需要处理
- `SUM(条件)` 在MySQL中可用，但标准SQL建议用WHERE过滤

## 明日计划
- 第三组：第N高/TopN问题（5道）
- 第四组：3表以上JOIN（5道）
- 第五组：综合子查询（5道）

## 累计进度
- 简单题：15道 ✅
- 中等题：12道 ✅
- 专项题：10道 ✅
- **总计：37道**
