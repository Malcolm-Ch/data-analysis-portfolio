# 数据分析学习笔记（完整版）

**学习周期**: 2026-08-03 ~ 2026-08-06（4天）  
**作者**: 数据分析学习记录  
**目标**: 从Excel基础到Python数据分析可视化全流程

---

## 目录

- [Day 1: Matplotlib基础 + 电商销售分析项目](#day-1)
- [Day 2: Seaborn进阶可视化 + 员工绩效分析](#day-2)
- [Day 3: Pandas进阶 — 多表关联与数据透视](#day-3)
- [Day 4: 时间序列分析 + Excel导出](#day-4)
- [附录：常见错误汇总](#附录)

---

## Day 1: Matplotlib基础 + 电商销售分析项目

**日期**: 2026-08-03  
**数据集**: ecommerce_sales.csv（电商订单数据）

### 1.1 基础图表

#### 柱状图（带数值标签）
```python
import matplotlib.pyplot as plt

depts = ['技术部', '销售部', '财务部']
salaries = [13666, 12000, 10000]

plt.figure(figsize=(8,5), dpi=150)
bars = plt.bar(depts, salaries, color=['#3498db', '#2ecc71', '#e74c3c'])

for bar in bars:
    height = bar.get_height()
    plt.text(bar.get_x() + bar.get_width()/2., height,
             f'{int(height)}', ha='center', va='bottom')

plt.title('各部门平均薪资对比', fontsize=14)
plt.xlabel('部门', fontsize=12)
plt.ylabel('薪资（元）', fontsize=12)
plt.ylim(0, 16000)
plt.show()
```

#### 折线图
```python
import numpy as np

x = np.arange(1, 6)
y = [85, 90, 78, 92, 88]

plt.figure(figsize=(8,5), dpi=150)
plt.plot(x, y, marker='o', linestyle='-', color='blue', label='成绩')
plt.title('学生成绩趋势')
plt.xlabel('月份')
plt.ylabel('分数')
plt.legend()
plt.grid()
plt.xticks(x)  # 只显示整数刻度
plt.show()
```

#### 饼图
```python
region_sales = df.groupby('地区')['销售额'].sum()

plt.pie(region_sales, labels=region_sales.index, 
        autopct='%1.1f%%', startangle=90)
plt.title('各地区销售额占比')
plt.show()
```

### 1.2 数据清洗

```python
df = pd.read_csv('sales_data.csv')
df = df.drop_duplicates()                          # 去重
df = df.dropna(subset=['单价', '地区'])             # 删除关键字段缺失
df = df[df['数量'] > 0]                            # 过滤异常值
df['日期'] = pd.to_datetime(df['日期'])              # 转换日期格式
```

### 1.3 分组聚合

```python
# 基础聚合
monthly_sales = df.groupby('month').aggregate({'amount': 'sum'})

# 多函数聚合
city_stats = df.groupby('城市').agg({
    '销售额': ['sum', 'mean', 'count'],
    '数量': 'sum'
})
```

### 1.4 项目核心发现

| 分析维度 | 关键发现 |
|---------|---------|
| 月度销售趋势 | 1月最高，4月低谷，6月回升 |
| 品类销售TOP5 | 手机数码遥遥领先 |
| 地区销售占比 | 西北、西南、华南占比较高 |
| 支付方式偏好 | 分布均匀，支付宝略高 |
| 退货率分析 | 家居日用退货率最高 |
| 用户复购分析 | 复购率仅11.3% |

---

## Day 2: Seaborn进阶可视化 + 员工绩效分析

**日期**: 2026-08-04  
**数据集**: employee_data.csv（300名员工）

### 2.1 散点图与透明度

```python
plt.scatter(df['工龄'], df['月薪'], alpha=0.6, color='#3498db')
```

**alpha参数**: 0~1，控制透明度。点重叠时颜色越深=数据越密集。

### 2.2 多子图布局

```python
fig, axes = plt.subplots(2, 2, figsize=(12, 10))

axes[0,0].scatter(df['工龄'], df['月薪'], color='#3498db', alpha=0.6)
axes[0,1].scatter(df['绩效评分'], df['完成项目数'], color='#e74c3c', alpha=0.6)
axes[1,0].bar(dept_counts.index, dept_counts.values, color='#2ecc71')
axes[1,1].hist(df['出勤率'], bins=10, color='#f39c12', edgecolor='white')

plt.tight_layout()  # 自动调整间距
plt.show()
```

**⚠️ 注意**: `plt.subplot()`（单个）vs `plt.subplots()`（多个，带s）

### 2.3 Seaborn箱线图

```python
import seaborn as sns

sns.boxplot(data=df, x='部门', y='月薪', 
            hue='部门', palette='Set2', legend=False)
plt.title('各部门薪资分布箱线图')
plt.show()
```

**箱线图解读**:
- 中间横线 = 中位数
- 箱子上沿 = Q3（75%分位）
- 箱子下沿 = Q1（25%分位）
- 圆点 = 异常值

### 2.4 热力图

```python
numeric_cols = ['工龄', '月薪', '绩效评分', '出勤率', '完成项目数']
corr_matrix = df[numeric_cols].corr()

sns.heatmap(corr_matrix, 
            annot=True, fmt='.2f', cmap='RdYlBu_r',
            center=0, square=True, linewidths=0.5)
plt.title('员工各项指标相关性热力图')
plt.show()
```

### 2.5 pairplot散点图矩阵

```python
# 基础版
sns.pairplot(df[['工龄', '月薪', '绩效评分', '完成项目数']], 
             diag_kind='kde', plot_kws={'alpha': 0.6})

# 按部门着色
sns.pairplot(df[['工龄', '月薪', '绩效评分', '完成项目数', '部门']], 
             hue='部门', palette='Set2')

# 晋升vs未晋升
sns.pairplot(df[['工龄', '月薪', '绩效评分', '是否晋升']], 
             hue='是否晋升', palette={'是': '#2ecc71', '否': '#e74c3c'})

# 只画左下角（不重复）
sns.pairplot(df[['工龄', '月薪', '绩效评分']], corner=True)
```

### 2.6 回归拟合散点图

```python
# 线性拟合
sns.regplot(data=df, x='工龄', y='月薪', 
            scatter_kws={'alpha': 0.5, 'color': '#3498db'},
            line_kws={'color': '#e74c3c', 'linewidth': 2})

# 多项式曲线拟合
sns.regplot(data=df, x='工龄', y='月薪', order=2,
            scatter_kws={'alpha': 0.5},
            line_kws={'color': '#e74c3c', 'linewidth': 2})

# 按部门分组回归
for dept in df['部门'].unique():
    subset = df[df['部门'] == dept]
    sns.regplot(data=subset, x='工龄', y='月薪',
                scatter_kws={'alpha': 0.4, 's': 30},
                line_kws={'linewidth': 2}, ci=None, label=dept)
plt.legend(title='部门')
```

### 2.7 核心发现

| 相关对 | 系数 | 结论 |
|--------|------|------|
| 工龄 ↔ 月薪 | **0.87** | 强正相关 |
| 工龄 ↔ 绩效 | **0.76** | 强正相关 |
| 绩效 ↔ 项目数 | **0.64** | 中等正相关 |
| 出勤率 ↔ 任何指标 | ≈0 | 完全无关 |

**晋升核心逻辑**: 绩效 > 工龄 > 月薪 > 项目数。出勤率完全不影响晋升。

---

## Day 3: Pandas进阶 — 多表关联与数据透视

**日期**: 2026-08-05  
**数据集**: users.csv + products.csv + orders.csv

### 3.1 merge多表关联

```python
# 读取三表
users = pd.read_csv('users.csv')
products = pd.read_csv('products.csv')
orders = pd.read_csv('orders.csv')

# 左连接（保留左表所有行）
df = orders.merge(users, on='用户ID', how='left')
df = df.merge(products, on='商品ID', how='left')
df['销售额'] = df['单价'] * df['数量']
```

**merge关键参数**:
| 参数 | 说明 |
|------|------|
| `on='列名'` | 按哪列关联 |
| `left_on` / `right_on` | 两表列名不同时 |
| `how='left'` | 左连接 |
| `how='inner'` | 内连接 |
| `how='outer'` | 外连接 |

### 3.2 pivot_table数据透视表

```python
# 基础用法
pivot = df.pivot_table(
    values='销售额',      # 要汇总的值
    index='城市',         # 行
    columns='类别',       # 列
    aggfunc='sum',        # 汇总方式
    fill_value=0          # 空值填0
)

# 进阶：会员等级 × 城市
pivot = df.pivot_table(
    values='销售额',
    index='会员等级',
    columns='城市',
    aggfunc='sum',
    fill_value=0
)
```

**关键参数**:
| 参数 | 说明 |
|------|------|
| `values` | 要聚合的数据列 |
| `index` | 行标签 |
| `columns` | 列标签 |
| `aggfunc` | 聚合函数 |
| `fill_value` | 填充空值 |
| `margins=True` | 添加行列总计 |

### 3.3 groupby高级聚合

```python
# 多函数聚合
city_stats = df.groupby('城市').agg({
    '销售额': ['sum', 'mean', 'count'],
    '数量': 'sum'
})

# 组内排名
df['城市内排名'] = df.groupby('城市')['销售额'].rank(ascending=False, method='min')

# 多列分组（必须用列表！）
df.groupby(['城市', '会员等级'])['销售额'].sum()
```

### 3.4 transform组内广播

```python
# 计算每个城市-会员等级的销售额
city_level_total = df.groupby(['城市', '会员等级'])['销售额'].transform('sum')

# 每个城市的总销售额
city_total = df.groupby('城市')['销售额'].transform('sum')

# 占比（直接赋值给原表）
df['等级城市占比'] = city_level_total / city_total * 100
```

**transform vs aggregate区别**:
| 方法 | 返回结果 | 行数 | 用途 |
|------|---------|------|------|
| `transform('sum')` | Series（广播值） | 与原表相同 | 在原表上加新列 |
| `aggregate('sum')` | DataFrame（汇总） | 等于组数 | 只看汇总结果 |

### 3.5 综合挑战：三维分析

```python
# 各城市各等级最爱的类别TOP1
cat_pref = df.groupby(['城市', '会员等级', '类别'])['销售额'].sum().reset_index()
cat_pref['类别排名'] = cat_pref.groupby(['城市', '会员等级'])['销售额'].rank(
    ascending=False, method='min'
)
top1 = cat_pref[cat_pref['类别排名'] == 1]
```

### 3.6 核心发现

| 发现 | 数据支撑 |
|------|---------|
| 上海普通会员占城市97.76% | 高等级会员挖掘空间巨大 |
| 苏州会员结构更健康 | 普通会员仅占33.94% |
| 手机数码是绝对王者 | 几乎所有城市×等级的TOP1 |
| 家居日用全等级低迷 | 2500-7000，可考虑换品 |

---

## Day 4: 时间序列分析 + Excel导出

**日期**: 2026-08-06  
**数据集**: orders_time.csv（2025年）+ orders_time_2024.csv（2024年）

### 4.1 时间序列基础

```python
# 读取并设置日期索引
df = pd.read_csv('orders_time.csv', parse_dates=['日期'])
df = df.set_index('日期')

# 月度汇总
monthly = df['销售额'].resample('ME').sum()

# 周度汇总
weekly = df['销售额'].resample('W').sum()
```

**resample常用规则**:
| 规则 | 含义 |
|------|------|
| `'D'` | 按天 |
| `'W'` | 按周 |
| `'ME'` | 按月（月末）|
| `'Q'` | 按季度 |
| `'Y'` | 按年 |

### 4.2 移动平均

```python
# 7日移动平均
daily = df['销售额'].resample('D').sum()
daily_ma7 = daily.rolling(window=7).mean()

# 可视化
plt.figure(figsize=(12, 5))
daily.plot(label='日销售额', alpha=0.5, color='gray')
daily_ma7.plot(label='7日移动平均', color='#e74c3c', linewidth=2)
plt.legend()
plt.title('销售额趋势（7日移动平均）')
plt.show()
```

### 4.3 同比环比分析

```python
# 环比（和上个月比）
mom = monthly.pct_change() * 100

# 同比（和去年同期比）
df_2024 = pd.read_csv('orders_time_2024.csv', parse_dates=['日期'])
df_2024 = df_2024.set_index('日期')
monthly_2024 = df_2024['销售额'].resample('ME').sum()

yoy = (monthly.values - monthly_2024.values) / monthly_2024.values * 100
yoy_series = pd.Series(yoy, index=monthly.index)
```

### 4.4 双Y轴图表

```python
fig, ax1 = plt.subplots(figsize=(10, 5))

# 左轴：环比（柱状图）
colors = ['#e74c3c' if x > 0 else '#2ecc71' for x in mom.dropna()]
ax1.bar(mom.dropna().index, mom.dropna(), color=colors, alpha=0.7, label='环比')
ax1.axhline(y=0, color='black', linestyle='-', linewidth=0.5)
ax1.set_ylabel('环比增长率 (%)', color='black')

# 右轴：同比（折线图）
ax2 = ax1.twinx()
ax2.plot(yoy_series.index, yoy_series, color='#3498db', marker='o', linewidth=2, label='同比')
ax2.set_ylabel('同比增长率 (%)', color='#3498db')

plt.title('2025上半年 环比 VS 同比增长率')
fig.legend(loc='upper right', bbox_to_anchor=(1.15, 1))
plt.tight_layout()
plt.show()
```

### 4.5 Excel导出

```python
# 单sheet导出
summary.to_excel('销售分析.xlsx', index=False, sheet_name='汇总')

# 多sheet导出
with pd.ExcelWriter('销售分析多sheet.xlsx', engine='openpyxl') as writer:
    summary.to_excel(writer, sheet_name='汇总', index=False)
    monthly_df.to_excel(writer, sheet_name='月度', index=False)
    weekly_df.to_excel(writer, sheet_name='周度', index=False)
```

### 4.6 Excel格式美化

```python
from openpyxl import load_workbook
from openpyxl.styles import Font, Alignment, PatternFill

wb = load_workbook('销售分析.xlsx')
ws = wb['汇总']

# 标题行加粗 + 蓝色背景 + 居中
for cell in ws[1]:
    cell.font = Font(bold=True, color='FFFFFF')
    cell.fill = PatternFill(start_color='3498DB', end_color='3498DB', fill_type='solid')
    cell.alignment = Alignment(horizontal='center')

# 自动调整列宽
for column in ws.columns:
    max_length = 0
    column_letter = column[0].column_letter
    for cell in column:
        try:
            if len(str(cell.value)) > max_length:
                max_length = len(str(cell.value))
        except:
            pass
    adjusted_width = min(max_length + 2, 20)
    ws.column_dimensions[column_letter].width = adjusted_width

wb.save('销售分析_美化版.xlsx')
```

### 4.7 核心发现

| 月份 | 环比 | 同比 | 解读 |
|------|------|------|------|
| 2月 | -5.2% | +43.8% | 环比下滑，同比大增 |
| 3月 | -20.7% | +39.8% | 节后淡季，环比暴跌 |
| 4月 | +5.1% | +27.2% | 环比转正 |
| 5月 | +14.2% | +58.8% | 618预热，双增长 |
| 6月 | -27.5% | +15.8% | 促销后回落 |

---

## 附录：常见错误汇总

| 错误 | 原因 | 修正 |
|------|------|------|
| `plt.subplot(2,2)` | 少s | `plt.subplots(2,2)` |
| `groupby('城市', '会员等级')` | 多列分组必须用列表 | `groupby(['城市', '会员等级'])` |
| `df.unique()` | unique是Series方法 | `df['列'].unique()` 或 `df.drop_duplicates()` |
| `monthly_2024.value` | values少s | `monthly_2024.values` |
| `resample('M')` | 已弃用 | `resample('ME')` |
| transform vs aggregate混淆 | 返回形状不同 | 要广播用transform，要汇总用aggregate |
| `hue`未指定 | 新版Seaborn要求 | `hue='部门'` |
| 覆盖原列导致map失效 | 数据污染 | 创建新列：`df['新列'] = df['原列'].map(...)` |
| Excel路径错误 | 用了服务器路径 | 本地用相对路径：`'文件名.xlsx'` |
| `PatternFill`未导入 | 漏import | `from openpyxl.styles import Font, Alignment, PatternFill` |

---

## 完整技能清单（4天累计）

### 可视化
- [x] `plt.bar()` / `plt.plot()` / `plt.scatter()` / `plt.pie()` / `plt.hist()`
- [x] `plt.title()` / `plt.xlabel()` / `plt.ylabel()` / `plt.legend()`
- [x] `plt.xticks(rotation=)` / `plt.ylim()` / `plt.grid()`
- [x] `plt.subplots()` 多子图布局 / `plt.tight_layout()`
- [x] `sns.boxplot()` + `hue` + `palette`
- [x] `sns.heatmap()` + `annot` + `cmap` + `center`
- [x] `sns.pairplot()` + `corner=True` + `diag_kind`
- [x] `sns.regplot()` + `order=2` + `scatter_kws` / `line_kws`
- [x] `alpha` 透明度 / `twinx()` 双Y轴

### 数据处理
- [x] `pd.read_csv()` + `parse_dates`
- [x] `df.set_index()` / `df.reset_index()`
- [x] `df.drop_duplicates()` / `df.dropna(subset=)`
- [x] `df[df['列'] > 0]` 异常值过滤
- [x] `df.groupby()` + `.agg()` / `.sum()` / `.count()` / `.rank()`
- [x] `df.groupby().transform()` 组内广播
- [x] `df.pivot_table()` 数据透视
- [x] `df.merge()` 多表关联（left join）
- [x] `df['列'].map()` 类别转数值
- [x] `df.resample('ME')` 时间重采样
- [x] `df.rolling(window=7).mean()` 移动平均
- [x] `df.pct_change()` 环比计算

### Excel导出
- [x] `df.to_excel()` 单sheet
- [x] `pd.ExcelWriter()` 多sheet
- [x] `openpyxl` 格式美化（字体/背景/对齐/列宽）

### 分析思维
- [x] 数据清洗 → 分析 → 可视化 → 业务结论
- [x] 同比环比分析
- [x] 相关性分析
- [x] 分组对比分析
- [x] 时间序列趋势分析
